# ============================================================
# Day 6 · 加固版 Dockerfile —— 骨架（只填空，不抄答案）
#
# 用法：cp Dockerfile.skeleton Dockerfile
#       然后把每个 【空N】 换成你的答案；每行后面的"菜单"是选项，选一个
#
# 推进法（重要，降低心智负担）：
#   第 1 遍：只写到阶段 A，目标 = build 成功 + run 起来 + curl 有响应
#   第 2 遍起：一次只加一个加固动作，每加一条就 build + run 一次
#            —— 增量验证，出了错只可能在新加的那行
# ============================================================

# ---------- 阶段 A：先让它"能跑"（= Day 3 的多阶段骨架）----------

# 【空1】构建阶段的基础镜像：______  AS builder
#         菜单：golang:1.25.1-alpine | alpine:3.22 | scratch
#         想清楚：go build 需要什么？哪个镜像里有它？

# 【空2】设置工作目录：______  /src
#         菜单：WORKDIR | COPY | ENV | RUN cd

# 【空3】先拷什么？（目标：改一行 main.go 不要重装依赖）
#         第 1 条 COPY：______（只拷几乎不变的文件）
#         第 2 条 RUN  ：______（下载依赖）
#         第 3 条 COPY：______（这时才把会变的拷进来）

# 【空4】编译：RUN ______ go build -o /out/webapp ./main.go
#         菜单：CGO_ENABLED=0 | CGO_ENABLED=1 | GOOS=linux
#         选哪个？为什么？（提示：runtime 阶段要选最小的底座。Day 3 已经验过）

# ---------- 阶段 B：加固 1 —— 换小的运行底座 ----------

# 【空5】运行阶段的基础镜像：______
#         菜单：alpine:3.22 | scratch | golang:1.25.1-alpine
#         ⚠️ 选完必须回答两问：
#           ① 这个底座里有 shell / wget 吗？→ 决定【空10】怎么写
#           ② 体积 vs 可排障性，你选哪个？（面试会问）

# ---------- 阶段 C：加固 2 —— 非 root ----------

# 【空6】建用户（选了 alpine 才有这一步）：RUN ______ app
#         菜单：adduser -D -u 10001 | useradd -m -u 10001 | groupadd
#         ⚠️ 如果【空5】选了 scratch：这行必须删掉 —— 因为 scratch 里
#            既没有 /etc/passwd 也没有 shell。那 USER 该写成什么？
#            【空6b】______（提示：内核只看数字）

# 【空7】拷产物，注意属主：
#         COPY ______=______:______ --from=builder /out/webapp /app/webapp
#         ⚠️ 不写这个参数会怎样？（属主是 root；755 还能执行，但目录不可写）

# 【空8】USER 这一行应该放在「拷产物」的【之前 / 之后】？______
#         为什么？______

# ---------- 阶段 D：加固 3+4 —— 让它"看起来就是生产版" ----------

# 【空9】EXPOSE ______
#         菜单：80 | 8080 | 1024
#         提示：今天实测结论 —— 非 root 进程绑低端口会被拒（P/E 被清空）

# 【空10】HEALTHCHECK 怎么写：______
#         可选方案：
#           a) 抽测 HTTP：用底座里现成的 wget/curl
#           b) exec form 抽测自己的二进制（需要程序支持某个参数）
#           c) 不写 —— 但要能说清代价
#         记住语义：退出码 0 = 健康，1 = 不健康

# 【空11】CMD 用哪种形式：______
#         菜单：exec form（JSON 数组） | shell form
#         为什么？（Day 5 的 PID 1 信号实验 + build 时的那条 warning）

# ============================================================
# 附加：.dockerignore（自己新建 `.dockerignore` 文件，至少考虑这几类）
#   [ ] .git             —— 体积 + 历史泄露
#   [ ] 本地编译产物 webapp —— 会覆盖镜像内编译的产物（Day 2 的坑）
#   [ ] *.log
#   [ ] README.md / 笔记
#   [ ] 任何密钥、证书、.env
# ============================================================
