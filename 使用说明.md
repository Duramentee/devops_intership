# 使用说明（只看这一个文件）

我在用 AI 当陪练学 DevOps / Kubernetes。这个文件夹就干这一件事：
**资料 → 转成 AI 能精准读的文本 → 讲给我听 → 沉淀成我的笔记。**

## 目录速查

| 目录 / 文件 | 是干嘛的 | 我要管吗 |
|---|---|---|
| `docs/` | 📚 **教材讲义**（`k8s_in_action/` 00~08、`linux/` 故障索引+每日一题、`github_actions_in_action/`）| **按顺序读** |
| `docs/00-知识点索引.md` | 🧭 **知识点跳转索引**（一行一个知识点 → 直达讲义小节；先覆盖 Docker，Linux/K8s 待补）| 想不起来「那个点在哪」时查 |
| `notes/` | ✍️ **我的笔记**（`weekN/dayN.md`，回看型结构）| **每天写** |
| `notes/错题本.md` | ✍️ **错题本**（跳周累积；按错因分类 + 复查记录）| 答错/答不全时追加 |
| `code/` | 💻 **我的代码**（`weekN/dayN/`，每天一个目录，放源码 / Dockerfile / YAML）| **每天写** |
| `plan/` | 🎯 **计划与进度**（六周计划 + `weekN/任务明细.md`）| 每周日勾进度 |
| `labs/` | 🧪 **老师实训作业**（`实践任务.md` + `shell/`）| **要做** |
| `assets/` | 🗄 **原始资料**（`pdf/` 原始 PDF、`text/` 转换出的可搜索文本；**AI 只读 text**）| ❌ 不用管 |
| `AGENTS.md` | 给 AI 定的规矩（自动加载，不用手动喂） | ❌ 不用管，想改风格再来 |
| `.tools/` | 转换脚本 + 依赖（隐藏目录） | ❌ **完全不用管** |
| `.trash/` | 整理时删掉的垃圾暂存 | ❌ 哪天顺眼删掉：`rm -rf .trash` |

> 一句话：**你只碰 `notes/`、`code/` 和 `labs/实践任务.md`，其余是给机器用的。**
>
> 每天固定两个落点：`notes/weekN/dayN.md`（笔记）+ `code/weekN/dayN/`（当天的代码）。

## 整体结构（一张图看懂）

```text
devops_intership/
├── 使用说明.md          ← 你在这（唯一入口）
├── AGENTS.md            ← AI 的行为规则
├── docs/                📚 教材讲义（只读）
│   ├── k8s_in_action/            00~08 模块教程
│   ├── linux/                    Linux 故障索引 + 每日一题
│   └── github_actions_in_action/ Actions 实战薄书
├── notes/               ✍️ 我的笔记      weekN/dayN.md
├── code/                💻 我的代码      weekN/dayN/
├── plan/                🎯 计划与进度     求职学习计划.md + weekN/任务明细.md
├── labs/                🧪 实训作业       实践任务.md + shell/
├── assets/              🗄 资料          pdf/（原始 PDF）+ text/（转换文本，AI 读这个）
├── .tools/              ⚙️ PDF→文本 转换脚本（别动）
└── .trash/              🗑 垃圾暂存（确认无用后 rm -rf .trash）
```

## 开始学习（按课程走）

`docs/k8s_in_action/` 里已经放好了**整套 K8s 教程**（从全书 18 章提炼）：

| 文件 | 内容 |
|---|---|
| `docs/k8s_in_action/00-学习路线图.md` | 总纲：一条主线 + 7 周计划 + 每章速览 + 版本提醒 |
| `docs/k8s_in_action/01` ~ `08` | 8 个模块，每个含：概念 / 细节 / 命令 / 易错点 / 动手实践 / 自测题 |

学习方式：**每天看一个模块的一节 → 动手做「动手实践」→ 做「自测题」→ 让 AI 讲解或批改。** 例如：

> 读 docs/k8s_in_action/05，给我讲 Deployment 滚动升级和回滚，然后出 5 道题考我

## 怎么开始学

不用记命令，直接对 `dsh` 说人话。下面是可直接抄的句式：

| 我想干的事 | 就这么说 |
|---|---|
| 开一个新主题 | `读 p.427 附近的「控制滚动升级速率」，用表格讲 maxSurge 和 maxUnavailable，然后出 3 题考我` |
| 先看章节在哪 | `Deployment 在第几章？给我页码` |
| 复习昨天学的 | `读 notes/week1/day1.md，考我 5 个问题` |
| 让它记笔记 | `把刚才这节的结论写进 notes/week1/day1.md` |
| 加一本新书 | `把 assets/pdf/新书.pdf 转成可搜索文本` |

提问三要点（照做，答案会准很多）：

1. **给坐标** —— 说页码或关键词，别只说主题（「讲 Deployment」→ 太宽）
2. **给环境** —— 报错原文 + 版本 + 期望结果
3. **指定格式** —— 「用表格」「逐字段解析」「先结论后依据」

## 自己查资料时（可选）

想不通过 AI 自己翻书：

| 目的 | 命令 |
|---|---|
| 找章节页码 | `grep -n "关键词" assets/text/k8s-in-action.index.md` |
| 找正文位置 | `grep -n "关键词" assets/text/k8s-in-action.txt` |
| 看某页内容 | 在 `assets/text/k8s-in-action.txt` 里搜 `=== PAGE 427 ===` |

小坑：这本书用「**滚动升级**」不是「滚动更新」，搜不到就换词根（`滚动`）或搜英文（`maxSurge`、`rollingUpdate`）。

## 笔记约定

`notes/weekN/dayN.md` 按「回看型结构」写（**不是**给第一次学用的教程），方便日后当手册查：

```markdown
> 头部：日期 / 代码目录 / 关联文档 / 产出

## 1. 结论速览      # 表格，5~8 条，只写结论
## 2. 机制          # 分小节；判定条件用表格
## 3. 命令          # 场景 | 命令 | 说明
## 4. 实测数据      # 原始输出（对照组 + 关键数字）
## 5. 易错点        # 错法 | 现象 | 正确做法
## 6. 每日一题      # 原答 + 判定 + 修正版
## 7. 遗留疑问      # 带进下次
```

> **不写**：今日任务清单、过程流水、明天起点这类教学过程记录（它们不影响回看）。
> **用词**：少用比喻、多用专业术语（例："能力上限"而不是"天花板"、"完全阻断"而不是"堵死"）。

### 错题本（`notes/错题本.md`）

答错、答不全、以及"答对了但机制说不清"的题，一律追加进去，四列：

```markdown
| # | 题目 | 我的错答（要避免） | 正确结论 | 错因 |
```

错因分五类：**A 概念混淆 / B 命令语义记错 / C 机制答不全 / D 知识空白 / E 实验设计**。
复习时遮住「正确结论」列逐条口述，说不出来的记入文末「复查记录」。
