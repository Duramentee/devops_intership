# 使用说明（只看这一个文件）

我在用 AI 当陪练学 DevOps / Kubernetes。这个文件夹就干这一件事：
**资料 → 转成 AI 能精准读的文本 → 讲给我听 → 沉淀成我的笔记。**

## 目录速查

| 目录 / 文件 | 是干嘛的 | 我要管吗 |
|---|---|---|
| `notes/` | ⭐ **我的学习笔记**，AI 讲完写在这里 | **要看、要复习** |
| `实践任务.md` | 老师给的 Linux 综合实训作业 | **要做** |
| `pdf/` | 原始资料（K8s 书、Git 速查表） | 存着就行 |
| `text/` | 转换后的可搜索文本，**AI 只读这里** | ❌ 不用管 |
| `AGENTS.md` | 给 AI 定的规矩（自动加载，不用手动喂） | ❌ 不用管，想改风格再来 |
| `.tools/` | 转换脚本 + 依赖（隐藏目录） | ❌ **完全不用管** |
| `.trash/` | 整理时删掉的垃圾暂存 | ❌ 哪天顺眼删掉：`rm -rf .trash` |

> 一句话：**你只碰 `notes/` 和 `实践任务.md`，其余是给机器用的。**

## 怎么开始学

不用记命令，直接对 `dsh` 说人话。下面是可直接抄的句式：

| 我想干的事 | 就这么说 |
|---|---|
| 开一个新主题 | `读 p.427 附近的「控制滚动升级速率」，用表格讲 maxSurge 和 maxUnavailable，然后出 3 题考我` |
| 先看章节在哪 | `Deployment 在第几章？给我页码` |
| 复习昨天学的 | `读 notes/week1/day1.md，考我 5 个问题` |
| 让它记笔记 | `把刚才这节的结论写进 notes/week1/day1.md` |
| 加一本新书 | `把 pdf/新书.pdf 转成可搜索文本` |

提问三要点（照做，答案会准很多）：

1. **给坐标** —— 说页码或关键词，别只说主题（「讲 Deployment」→ 太宽）
2. **给环境** —— 报错原文 + 版本 + 期望结果
3. **指定格式** —— 「用表格」「逐字段解析」「先结论后依据」

## 自己查资料时（可选）

想不通过 AI 自己翻书：

| 目的 | 命令 |
|---|---|
| 找章节页码 | `grep -n "关键词" text/k8s-in-action.index.md` |
| 找正文位置 | `grep -n "关键词" text/k8s-in-action.txt` |
| 看某页内容 | 在 `text/k8s-in-action.txt` 里搜 `=== PAGE 427 ===` |

小坑：这本书用「**滚动升级**」不是「滚动更新」，搜不到就换词根（`滚动`）或搜英文（`maxSurge`、`rollingUpdate`）。

## 笔记约定

`notes/week1/dayN.md` 固定四段，方便复习：

```markdown
## 概念      # 这节到底在讲什么
## 命令      # 用表格：场景 | 命令 | 说明
## 易错点    # 我踩过的坑
## 我的疑问  # 没搞懂的先记下，下次接着问
```
