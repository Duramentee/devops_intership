# 知识点索引（可跳转）

> **它的作用**：讲义按主题分文件，而你脑子里的问题是「**XX 那个点在哪**」。这一页给出答案：一行一个知识点，点右边链接直接跳到对应小节。
>
> | 你的情况 | 怎么用 |
> |---|---|
> | 记得关键词 | `Ctrl+F` 搜**本页**（全部知识点都在这一页） |
> | 不记得关键词 | 走 §3「按场景」或 §4「按现象」 |
> | 链接没跳准（锚点偶尔失效） | 看链接文字里的节号（如 `01 §10.3`），在目标文件里 `Ctrl+F` 搜 `10.3` |
>
> **覆盖范围**：✅ Docker（`docs/docker/` 4 篇） · ⚪ Linux / K8s / Actions → 见 §6（待补）
> **维护**：改了讲义标题后回来改链接，改完**跑一次自检**（锚点算错会静默失效，不报错）：
>
> ```bash
> node .tools/check-anchor-links.mjs docs docs/00-知识点索引.md
> ```
>
> 该脚本的 slug 规则已用官方 `github-slugger` 在 `docs/` 全部标题上逐字比对（差异 0 条）。

---

## 0. 一分钟速记（先背这几条，其余随用随查）

| # | 结论 | 展开在 |
|---|---|---|
| 1 | 容器 = **namespace（限制能看到什么）+ cgroup（限制能用多少资源）** 的普通 Linux 进程，与宿主机**共用内核** | [01 §五/§六](docker/01-架构与原理.md#五namespace隔离看得见什么) |
| 2 | 镜像 = **一组只读层**；容器启动时在最上面加一个**可写层** | [01 §七](docker/01-架构与原理.md#七镜像分层与写时复制) |
| 3 | **Dockerfile 每条指令 = 一层**；改动层之后的层全部重建（缓存按层命中） | [01 §八](docker/01-架构与原理.md#八构建缓存规则) · [03 §四](docker/03-Dockerfile词典.md#四缓存与顺序) |
| 4 | **运行**镜像得到**容器**；容器删掉，可写层里的数据就没了（要持久化用 volume） | [02 §五](docker/02-命令词典.md#五存储) |
| 5 | **构建时的事用 `RUN`，运行时的事用 `CMD`/`ENTRYPOINT`** | [03 §三](docker/03-Dockerfile词典.md#三三个最易混run-vs-cmd-vs-entrypoint) |
| 6 | 排障顺序：**在不在 → 为什么退出 → 日志 → 环境 → 资源** | [04 §一](docker/04-排障索引.md#一五步定位法无条件按顺序走) |
| 7 | 容器权限 = **uid + 一组 capability 位**；容器内 root 风险较低是因为 namespace 限制可见范围 + capability 削减能力；**一旦逃逸就等价于宿主 root** | [01 §十](docker/01-架构与原理.md#十capability容器里的-root-为什么不是宿主-root) |

---

## 1. 文件清单（按需查，不必从头读）

| 文件 | 内容 | 什么时候查 |
|---|---|---|
| [`docker/01-架构与原理.md`](docker/01-架构与原理.md) | 三大角色与启动链路、镜像/容器/仓库、namespace、cgroup、overlay2 分层与写时复制、构建缓存、capability 权限机制 | 想弄懂"为什么会这样"；答面试题 |
| [`docker/02-命令词典.md`](docker/02-命令词典.md) | 镜像 / 容器 / 网络 / 存储 / 清理五组命令 + `docker run` 选项表 + 输出字段速查 | **写命令时**查（最常用） |
| [`docker/03-Dockerfile词典.md`](docker/03-Dockerfile词典.md) | 指令语义全表、RUN/CMD/ENTRYPOINT 对比、缓存与指令顺序、多阶段构建、反模式、`USER`/UID | **写 Dockerfile 时**查（最常用） |
| [`docker/04-排障索引.md`](docker/04-排障索引.md) | 五步定位法、退出码速查（含每个码的第一步命令）、现象索引主表、报错前缀、诊断命令组合 | 容器出问题时**第一个**打开 |

**标注约定**

| 标记 | 含义 |
|---|---|
| 📖 | 书本原文 / 书本观点，标注节号 + 正文行号可回查 |
| ✍️ | 我的补充（书本没有或说法已过时），可以质疑、可以自己验 |
| ⚠️ | 易错点 |

> ⚠️ 引用 K8s 书时的**页码陷阱**：`assets/text/k8s-in-action.index.md` 的页码列 ≠ 正文的 `=== PAGE n ===` 编号。
> **回查一律用「节号 + 正文行号」**（例：2.1.4 构建镜像 → `k8s-in-action.txt` 行 3104-3148）。

---

## 2. 按主题

### A · 架构与角色（`docker/01-架构与原理.md`）

| 知识点 | 一句话结论 | 直达 |
|---|---|---|
| 三大角色与启动链路 | CLI 只发请求 → dockerd → containerd → runc；`docker run` 的五步 | [01 §一](docker/01-架构与原理.md#一三大角色与启动链路) |
| 三大概念 | 镜像（只读模板）/ 容器（运行实例）/ 仓库；`registry/namespace/name:tag` | [01 §二](docker/01-架构与原理.md#二三大概念) |
| 关键路径与端口 | `/var/run/docker.sock`、`/var/lib/docker`、`docker info` | [01 §三](docker/01-架构与原理.md#三关键路径与端口排障常用) |
| Docker 与 K8s 的分工 | Docker 管单机，K8s 管集群；K8s 1.24+ 用 containerd 而不是 dockerd | [01 §四](docker/01-架构与原理.md#四docker-与-kubernetes-的分工) |

### B · 隔离与限制（`docker/01-架构与原理.md`）

| 知识点 | 一句话结论 | 直达 |
|---|---|---|
| namespace | 隔离进程能看到什么：pid / net / mnt / uts / ipc / user | [01 §五](docker/01-架构与原理.md#五namespace隔离看得见什么) |
| cgroup | 限制进程能用多少：CPU / 内存 / IO / 进程数；超内存 OOMKill → 退出码 137 | [01 §六](docker/01-架构与原理.md#六cgroup限制能用多少资源) |

### C · 镜像分层与构建缓存（`01` / `03`）

| 知识点 | 一句话结论 | 直达 |
|---|---|---|
| 分层结构（overlay2） | 镜像 = 一组只读层，容器启动时最上面加可写层 | [01 §七](docker/01-架构与原理.md#七镜像分层与写时复制) |
| 写时复制（CoW） | 改文件先从下层复制到可写层（copy-up），下层原文件不变 | [01 §七 CoW](docker/01-架构与原理.md#写时复制copy-on-write) |
| 构建缓存规则 | 按层比对；输入变了就从该层起往后全部重建 | [01 §八](docker/01-架构与原理.md#八构建缓存规则) |
| 容器进程 vs 宿主进程 | 同一批进程，两侧 PID 不同（PID namespace 改变的是视角） | [01 §九](docker/01-架构与原理.md#九容器进程-vs-宿主进程) |
| 构建流程 | Dockerfile → 构建上下文 → 逐层执行 → 每层固化 | [03 §一](docker/03-Dockerfile词典.md#一构建流程) |
| 缓存与指令顺序 | 依赖清单单独一层、放在 `COPY . .` **之前** | [03 §四](docker/03-Dockerfile词典.md#四缓存与顺序) |
| 多阶段构建 | 多个 `FROM`，`COPY --from=` 只取产物，builder 阶段不进最终镜像 | [03 §六](docker/03-Dockerfile词典.md#六多阶段构建) |

### D · Dockerfile 怎么写（`docker/03-Dockerfile词典.md`）

| 知识点 | 一句话结论 | 直达 |
|---|---|---|
| 指令全表 | `FROM`/`RUN`/`COPY`/`WORKDIR`/`EXPOSE`/`USER`/`CMD`… 逐条语义 | [03 §二](docker/03-Dockerfile词典.md#二指令全表) |
| RUN vs CMD vs ENTRYPOINT | RUN 是**构建时**；CMD/ENTRYPOINT 是**运行时**，ENTRYPOINT 优先级更高 | [03 §三](docker/03-Dockerfile词典.md#三三个最易混run-vs-cmd-vs-entrypoint) |
| exec form vs shell form | `CMD ["a","b"]` 直接 exec；`CMD a b` 会套一层 `/bin/sh -c`（拦截信号） | [03 §三](docker/03-Dockerfile词典.md#exec-form-vs-shell-form) |
| `.dockerignore` | 与 `.gitignore` 同源，减少上下文体积并防止泄露 | [03 §五](docker/03-Dockerfile词典.md#五dockerignore常被忘) |
| 反模式清单 + 写完自查 | `apt upgrade`、`latest`、密钥烤进镜像…；表尾有 3 个补充自检问题 | [03 §七](docker/03-Dockerfile词典.md#七反模式清单与自查) |
| `USER` 与用户/组、UID 怎么选 | `adduser` ≠ `useradd`；UID 数字无内核含义，10001 是社区惯例 | [03 §八](docker/03-Dockerfile词典.md#八user-与用户组adduser-vs-useradduid-怎么选) |
| `useradd` vs `adduser` | `useradd` = shadow 原语；Debian 的 `adduser` = Perl 封装；**Alpine 只有 `adduser`** | [03 §8.1](docker/03-Dockerfile词典.md#81-useradd-与-adduser-是两个命令) |
| UID / GID 数字的含义 | 内核只认 `0` 和 `4294967295`，其余是发行版约定 | [03 §8.2](docker/03-Dockerfile词典.md#82-数字的含义uid--gid-与镜像里的号) |
| `USER` 的三个易错点 | 只写 uid 时 gid 仍是 0；`COPY` 的属主只由 `--chown` 决定 | [03 §8.3](docker/03-Dockerfile词典.md#83-user-的三个易错点) |

### E · 权限与安全（`docker/01-架构与原理.md` §十）

| 知识点 | 一句话结论 | 直达 |
|---|---|---|
| capability 总览 | 权限 = uid（只是一个数字）+ 一组 capability 位，判定看位图 | [01 §十](docker/01-架构与原理.md#十capability容器里的-root-为什么不是宿主-root) |
| 三层限制机制 | namespace 限制可见范围 + capability 削减能力 + seccomp/AppArmor 过滤 syscall | [01 §10.1](docker/01-架构与原理.md#101-三层限制机制) |
| `Cap*` 五个字段 | `Bnd` = 能力上限；`Prm`/`Eff` = 当前可用/生效；`Inh`/`Amb` = 跨 exec 传递 | [01 §10.2](docker/01-架构与原理.md#102-procpidstatus-里的-5-个-cap-字段) |
| 位图怎么读 | `0xa80425fb` = 默认 **14 个** capability，逐字节解码与清单一致 | [01 §10.3](docker/01-架构与原理.md#103-位图怎么读) |
| 逃逸路径 | `docker.sock` / `--privileged` **不是漏洞**，是配置直接绕过限制 | [01 §10.4](docker/01-架构与原理.md#104-逃逸路径) |
| 案例：非 root 也能 `ping` | ICMP 有两种实现路径：raw（需 `NET_RAW`）与 datagram（看 `ping_group_range`） | [01 §10.5](docker/01-架构与原理.md#105-案例非-root-也能-ping--icmp-的两种实现路径) |
| 案例：绑低端口由谁决定 | 判定条件 = **端口 < `ip_unprivileged_port_start`** 且 **`CapEff` 无该位** | [01 §10.6](docker/01-架构与原理.md#106-案例绑低端口的权限由什么决定) |
| ⚠️ Docker 放宽的 sysctl 默认值（§10.7） | `ping_group_range=0 …`、`ip_unprivileged_port_start=0` → **默认值 ≠ 最小权限** | [01 §十](docker/01-架构与原理.md#十capability容器里的-root-为什么不是宿主-root)（小节标题含 emoji，锚点不稳定，进去搜 `10.7`） |
| 常见误区 | 4 条高频错误认知及其纠正 | [01 §十一](docker/01-架构与原理.md#十一常见误区) |

### F · 命令（`docker/02-命令词典.md`）

| 知识点 | 一句话结论 | 直达 |
|---|---|---|
| 镜像相关 | build / pull / tag / history / inspect / save / load / prune | [02 §一](docker/02-命令词典.md#一镜像相关) |
| 容器生命周期 | run = create + start；stop 发 SIGTERM，超时后升级为 SIGKILL | [02 §二](docker/02-命令词典.md#二容器生命周期) |
| 查看与排障 | ps / logs / inspect / exec / top / stats / diff / events | [02 §三](docker/02-命令词典.md#三查看与排障) |
| 网络 | 自定义网络 + 内嵌 DNS；bridge / host / none / container: | [02 §四](docker/02-命令词典.md#四网络) |
| 存储 | 命名卷 / 绑定挂载 / tmpfs / `--mount` 写法 | [02 §五](docker/02-命令词典.md#五存储) |
| 系统与清理 | `system df` 看占用；各 `prune` 的清理范围 | [02 §六](docker/02-命令词典.md#六系统与清理) |
| `docker run` 选项大表 | 高频选项一页扫完（含 `--cap-*`、`--sysctl`、`--read-only`） | [02 §七](docker/02-命令词典.md#七docker-run-选项大表最高频) |
| 输出怎么读（逐字段） | `ps` / `inspect` / `history` 的列与关键字段 | [02 §八](docker/02-命令词典.md#八输出怎么读逐字段) |
| 易错点：选项的"归属" | 哪个参数被本地 shell 解析、哪个传给容器内 | [02 §九](docker/02-命令词典.md#九易错点选项的归属与表达方式) |
| 9.1 三段式 | `docker run [选项] 镜像 [容器内命令]` 的分界规则 | [02 §9.1](docker/02-命令词典.md#91-三段式谁在解析哪个参数) |
| 9.2 高频坑 | `CAP_` 前缀、drop/add 顺序、`-u` 与 `USER`、默认值掩盖实验 | [02 §9.2](docker/02-命令词典.md#92-高频坑) |
| 9.3 `sh -c` 何时才需要 | 需要管道 / 重定向 / 多命令时才用 | [02 §9.3](docker/02-命令词典.md#93-sh--c-什么时候才需要) |
| 9.4 `-f '{{...}}'` 是 Go 模板 | 由本地 CLI 渲染，字段不存在会报 template error | [02 §9.4](docker/02-命令词典.md#94--f--是-go-模板) |

### G · 排障（`docker/04-排障索引.md`）

| 知识点 | 一句话结论 | 直达 |
|---|---|---|
| 五步定位法 | 在不在 → 为什么退 → 日志 → 环境 → 资源 | [04 §一](docker/04-排障索引.md#一五步定位法无条件按顺序走) |
| 退出码速查 | 10 个退出码 ×（谁的问题 · 常见原因 · 第一步命令） | [04 §二](docker/04-排障索引.md#二退出码速查) |
| ⚠️ 容器已退出时哪些命令不能用（§二） | `exec`/`stats`/`top` 失效，改用 `--entrypoint` 重开或 `inspect`/`cp` | [04 §二](docker/04-排障索引.md#二退出码速查) |
| ⚠️ 三条颠覆直觉的实测结论（§二） | `docker stop` 有 4 种可能结局；`127` 有两种形态；三个"工具会骗你"的点 | [04 §二](docker/04-排障索引.md#二退出码速查) |
| curl 退出码 | 客户端侧的另一套编码（7 vs 56 怎么区分） | [04 §二](docker/04-排障索引.md#curl-退出码客户端侧的另一套编码) |
| 现象索引表（主表） | 30+ 现象 → 先想哪个机制 → 第一条命令 | [04 §三](docker/04-排障索引.md#三现象索引表主表) |
| 报错前缀 = 谁在说话 | `OCI runtime exec failed`(runc) / `Error response from daemon`(dockerd) / 应用 | [04 §三](docker/04-排障索引.md#报错前缀--谁在说话) |
| 常用诊断命令组合 | 成套命令，可直接复制 | [04 §四](docker/04-排障索引.md#四常用诊断命令组合) |
| 极端情况 | 镜像没有 shell、容器卡 D 状态、`prune` 的边界 | [04 §五](docker/04-排障索引.md#五极端情况) |

### H · 自测题

| 知识点 | 位置 |
|---|---|
| 架构与原理自测（6 题） | [01 自测](docker/01-架构与原理.md#自测) |
| Dockerfile 自测（4 题） | [03 自测](docker/03-Dockerfile词典.md#自测) |

---

## 3. 按场景（干活时）

| 我要做什么 | 去哪 | 直达 |
|---|---|---|
| 写一个 Dockerfile | 指令全表 → 分层顺序 → 反模式自查 | [03 §二](docker/03-Dockerfile词典.md#二指令全表) |
| 镜像太大要瘦身 | 分层原理 + 多阶段构建 | [03 §六](docker/03-Dockerfile词典.md#六多阶段构建) |
| 改代码后不想重装依赖 | 缓存与指令顺序 | [03 §四](docker/03-Dockerfile词典.md#四缓存与顺序) |
| 查某条命令怎么用 | 命令分组 + run 选项大表 | [02 §七](docker/02-命令词典.md#七docker-run-选项大表最高频) |
| 命令写不通（引号 / 重定向） | 三段式 + `sh -c` | [02 §9.1](docker/02-命令词典.md#91-三段式谁在解析哪个参数) |
| 容器数据要保住 | 卷 / 绑定挂载 / 生命周期 | [02 §五](docker/02-命令词典.md#五存储) |
| 两个容器按名字互访 | 自定义网络 + 内嵌 DNS | [02 §四](docker/02-命令词典.md#四网络) |
| 把容器做安全 | 非 root + 最小 capability + 只读根 | [01 §十](docker/01-架构与原理.md#十capability容器里的-root-为什么不是宿主-root) |
| 看懂 `docker inspect` 输出 | 字段逐项解释 | [02 §八](docker/02-命令词典.md#八输出怎么读逐字段) |
| 磁盘满了 | 占用构成 + 清理边界 | [02 §六](docker/02-命令词典.md#六系统与清理) |

---

## 4. 按现象（出问题时）

> 不用背这张表：30+ 条现象在主表里，出问题直接翻它。

| 现象 | 去哪 |
|---|---|
| 容器起来就退出 / `Exited (n)` | [04 §二 退出码表](docker/04-排障索引.md#二退出码速查) + [04 §三 主表](docker/04-排障索引.md#三现象索引表主表) |
| 状态是 `Created`（没有 `Exited`） | [04 §三 主表](docker/04-排障索引.md#三现象索引表主表)（runc create 阶段就失败） |
| `Up (unhealthy)` | [04 §三 主表](docker/04-排障索引.md#三现象索引表主表)（不是退出，是 HEALTHCHECK 失败） |
| `curl localhost:8080` 不通 | [04 §三 主表](docker/04-排障索引.md#三现象索引表主表) + [curl 退出码 7/56](docker/04-排障索引.md#curl-退出码客户端侧的另一套编码) |
| `docker exec` 报找不到 `sh` | [04 §三 主表](docker/04-排障索引.md#三现象索引表主表)（scratch/distroless 镜像没有 shell） |
| 报错看不懂是哪一层 | [报错前缀 = 谁在说话](docker/04-排障索引.md#报错前缀--谁在说话) |
| 改了代码但镜像没变 | [04 §三 主表](docker/04-排障索引.md#三现象索引表主表)（`CACHED` / 起的是旧容器） |
| 数据"重启就丢" | [02 §五](docker/02-命令词典.md#五存储) + [01 §七](docker/01-架构与原理.md#七镜像分层与写时复制) |

---

## 5. 按面试点（高频）

| 面试题 | 答案骨架在哪 |
|---|---|
| 容器和虚拟机的区别 | [01 §五/§六](docker/01-架构与原理.md#五namespace隔离看得见什么) + [01 §四](docker/01-架构与原理.md#四docker-与-kubernetes-的分工) |
| 镜像分层的原理、为什么要分层 | [01 §七](docker/01-架构与原理.md#七镜像分层与写时复制) |
| 写时复制怎么实现 | [01 §七 CoW](docker/01-架构与原理.md#写时复制copy-on-write) |
| 为什么要多阶段构建、怎么压小镜像 | [03 §六](docker/03-Dockerfile词典.md#六多阶段构建) |
| 构建缓存怎么算、怎么提高命中率 | [01 §八](docker/01-架构与原理.md#八构建缓存规则) + [03 §四](docker/03-Dockerfile词典.md#四缓存与顺序) |
| CMD 和 ENTRYPOINT 的区别 | [03 §三](docker/03-Dockerfile词典.md#三三个最易混run-vs-cmd-vs-entrypoint) |
| 容器里是 root 安全吗 | [01 §十](docker/01-架构与原理.md#十capability容器里的-root-为什么不是宿主-root) + [01 §10.4](docker/01-架构与原理.md#104-逃逸路径) |
| 容器怎么限制内存 / CPU | [01 §六](docker/01-架构与原理.md#六cgroup限制能用多少资源) + [02 §七](docker/02-命令词典.md#七docker-run-选项大表最高频) |
| 容器数据怎么持久化 | [02 §五](docker/02-命令词典.md#五存储) |
| 容器之间怎么通信 | [02 §四](docker/02-命令词典.md#四网络) |
| 容器起不来你怎么查 | [04 §一](docker/04-排障索引.md#一五步定位法无条件按顺序走) + [04 §三](docker/04-排障索引.md#三现象索引表主表) |

---

## 6. 待补（以后往里加）

| 主题 | 状态 | 素材现址 |
|---|---|---|
| Linux（现象 → 机制 → 命令 + 每日一题） | ⚪ 待建 | `docs/linux/Linux-故障索引.md`、`Linux-每日一题.md` |
| Kubernetes（Pod / Service / 探针 / RBAC…） | ⚪ 待建（第 2 周起） | `docs/k8s_in_action/00`~`08` |
| GitHub Actions（Session 1-14 + Lab 1-8） | ⚪ 待建（第 5 周） | `docs/github_actions_in_action/` |
| 我的笔记（按天） | ⚪ 可选 | `notes/weekN/dayN.md` |
